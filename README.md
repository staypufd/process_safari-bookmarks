# process_safari-bookmarks
A small project using AppleScript to take a Safari Bookmarks file and sort each of the nested folders and items in each folder so that you can reimport them in sorted order.
